## 2단계 기획및 설계

#### 기능 목록 만들기
#### [MUST] 과목 입력받기
#### [MUST] 4.3 4.5 학점 각각 출력하기
#### [MUST] 목표달성을 위한 필요학점 출력하기
#### [MUST] 전공 부전공 교양 수강학점 각각 보여주기

#### 데이터 설계(리스트,딕셔너리)
#### SAMPLE_DATA = [ {"name": "일반수학1", "semester": "1-1", "type": "교양", "credit": 3, "grade_43": 4.0, "letter": "A0"},{"name": "일반물리학1", "semester": "1-1", "type": "교양", "credit": 3, "grade_43": 4.3, "letter": "A+"}]

#### 함수설계 
#### 먼저 수강한과목 
####import json
import os


# [공통 데이터 및 설정]

FILENAME = "grades.json"
my_subjects = []  # 수강 과목 데이터가 누적되는 전역 리스트


# ==========================================
# 1. 파일 입출력 및 초기화 기능
# ==========================================

def save_file_data():
    # 1. 현재까지 리스트(my_subjects)에 쌓인 성적 데이터를 JSON 파일로 저장한다
    pass


def load_file_data():
    # 1. 프로그램을 시작할 때 이전 파일(grades.json)을 불러온다
    # 2. 파일이 없거나 비어있으면 준비된 샘플 데이터를 기본으로 리스트에 세팅한다
    pass



# ==========================================
# 2. 데이터 관리 기능 (C.R.D)
# ==========================================

def input_new_subject():
    # 1. 사용자로부터 과목명, 학기, 구분, 학점, 성적 기호를 입력받는다
    # 2. 각 단계별 오타 및 타입 예외 처리(try-except)를 검증한다
    # 3. 검증된 새 과목 정보를 딕셔너리로 만든다
    # 4. 전체 리스트(my_subjects)에 추가한다
    # 5. 파일에 저장한다
    pass


def print_all_subjects():
    # 1. 리스트가 비어있는지 확인 후, 데이터가 있다면 이수 학기 및 구분 순으로 정렬한다
    # 2. 정렬된 데이터를 포맷에 맞춰 화면에 순서대로 출력한다
    pass


def delete_one_subject():
    # 1. 정렬된 전체 과목 리스트를 화면에 먼저 보여준다
    # 2. 사용자에게 삭제하려는 과목의 번호를 입력받는다
    # 3. 입력받은 번호에 해당하는 과목을 원본 리스트(my_subjects)에서 안전하게 지운다
    # 4. 변경된 리스트를 파일에 저장한다
    pass



# ==========================================
# 3. 데이터 연산 및 분석 기능
# ==========================================

def show_my_dashboard():
    # 1. 수강 과목들을 전공/부전공/교양 카테고리별로 분류한다
    # 2. 각 카테고리별 이수 학점과 가중 평균 평점(4.3 및 4.5 만점)을 계산한다
    # 3. 졸업 요건 학점과 비교하여 현재 이수 완료 현황 및 부족한 학점을 출력한다
    pass


def run_simulation():
    # 1. 졸업 기준 학점 대비 현재 이수 학점 현황을 브리핑한다
    # 2. 사용자로부터 최종 목표 졸업 평점, 남은 학기 수, 학기당 계획 학점을 입력받는다
    # 3. 목표 점수 도달을 위해 '앞으로 매 학기 받아야 하는 평균 평점'을 역산한다
    # 4. 계산 결과에 따라 달성 가능 여부와 가이드라인 멘트를 화면에 보여준다
    pass



# ==========================================
# 4. 메인 프로그램 엔진 제어
# ==========================================

def main():
    # 1. 프로그램 시작 시 load_file_data()를 호출하여 기존 성적을 자동 로드한다
    # 2. 무한 루프를 돌며 기능 선택 메뉴판(1~6)을 출력한다
    # 3. 사용자가 선택한 번호에 맞춰 위의 설계된 함수들을 분기 호출한다
    # 4. 6번(안전 종료)을 누르면 강제 저장을 수행한 뒤 루프를 탈출한다
    pass


if __name__ == "__main__":
    main()